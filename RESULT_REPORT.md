## 2026-06-22：轻量生存闭环插件

本轮新增 `src/survival-loop.js`，先交付低风险的小闭环：背包数量、放置消耗、挖掘入包、快捷栏数量角标、合成面板。

### 已完成

1. 新增独立生存模块，不重写 `src/v3.js` 主引擎。
2. 背包数量保存到 `block-world-survival-inventory-v1`，不破坏原有世界存档。
3. 初始提供少量基础方块，避免新世界无法开始。
4. 挖掘成功后根据游戏 toast 增加对应物品数量；水暂不加入背包。
5. 放置前检查当前方块数量，数量不足时阻止放置并提示。
6. 放置成功后扣除对应数量。
7. 快捷栏和物品栏显示数量。
8. 顶部新增 ⚒️ 合成 / 背包按钮。
9. 支持原木转木板、木板转工作台、木板转木棍、木镐、石镐、灯块等简化配方。
10. 新模块加载失败时只输出控制台警告，不影响主游戏启动。

### 已知限制

- 本轮没有直接接入 Three.js 世界实体系统，所以掉落物暂时不是 3D 小方块，而是挖掘后直接进入背包。
- 工具已可合成和计数，但尚未影响挖掘速度、采集等级和耐久。
- 当前执行环境不能联网安装依赖，因此未在本地运行 `npm run build`。

# 方块世界 3.0 优化报告

这次更新目标是把原型小游戏升级成更接近体素沙盒的版本，重点放在地图规模、交互、视觉氛围、物品栏、天气、动植物和移动端可玩性。

## 2026-06-22：AABB 后遗症修复

- 修复纵向移动落地判定，避免侧面高墙/树干被误当作地面。
- 修复上升碰撞判定，避免侧面擦碰时异常改动玩家高度。
- 树叶和松叶保留渲染实体属性，但玩家碰撞会忽略它们。
- 已执行 `npm run build`，Vite 构建通过。

## 2026-06-22：模块 19 真实 AABB 玩家碰撞、模块 30 区块 GPU 资源清理

1. 玩家碰撞从地面高度采样改为真实 AABB：宽 0.6、高 1.8。
2. 保持 `player.y` 作为眼睛高度坐标。
3. 新增玩家碰撞辅助函数。
4. `updatePlayer` 改为 X、Z、Y 分轴移动。
5. 放置方块时继续使用玩家 AABB 禁止把方块放进玩家碰撞盒。
6. 新增区块 mesh 资源清理函数。
7. `buildChunkMesh` 重建前释放旧 mesh；`disposeChunk` 卸载时释放 geometry 并清空 mesh 引用。
8. 没有释放全局复用材质。
9. 已运行 `npm run build`，构建通过。

## 2026-06-22：模块 20 移动端 WebGL 启动保护

1. `index.html` 继续使用 Vite 本地模块，没有引入 Three.js CDN。
2. 新增 `window.__blockWorldEngineGuard` 启动守卫。
3. 启动前使用测试 canvas 检测 WebGL / WebGL2 上下文。
4. `src/v3.js` 改为动态导入，模块加载失败时显示中文错误层。
5. 增加 `error`、`unhandledrejection`、`webglcontextlost` 和 `webglcontextrestored` 处理。

## 已完成的 48 项优化

1. 新增 `src/v3.js`，作为 3.0 主游戏引擎。
2. 新增 `src/v3.css`，重做移动端 HUD、物品栏、快捷栏和按钮样式。
3. `index.html` 切换到 3.0 入口。
4. 增加完整物品栏入口：顶部 🎒。
5. 增加物品栏弹窗。
6. 增加物品分类标签：全部、自然、植物、矿物、建筑。
7. 增加物品图标样式。
8. 增加底部快捷栏。
9. 增加数字键 1-9 切换快捷栏方块。
10. 增加 30+ 种方块和物品定义。
11. 增加草方块、泥土、石头、沙子、砂砾、黏土、雪、冰、水。
12. 增加原木、白桦木、树叶、松叶、仙人掌、芦苇、草丛、红花、黄花、蘑菇。
13. 增加煤矿、铁矿、金矿、晶簇、玄武岩、黑曜石。
14. 增加木板、砖块、玻璃、灯块、白墙、深色石、蓝砖、红砖。
15. 增加更大的区块地图系统。
16. 增加玩家移动时自动加载附近区块。
17. 增加远离玩家区块自动卸载，减少性能压力。
18. 优化体素渲染：只生成可见面。
19. 区分实体方块和透明/植物方块渲染。
20. 增加玻璃、水、植物等透明/非实体处理。
21. 增加地形高度噪声。
22. 增加山丘、细节和山脊变化。
23. 增加河流生成。
24. 增加多条河流合流的地形逻辑。
25. 增加海滩/水边地形。
26. 增加森林、平原、沙漠、湿地、雪地、冰河等生物群系。
27. 增加雪地随温度和高度生成。
28. 增加冷区水面结冰。
29. 增加森林树木随机生成。
30. 增加松树和普通树的差异。
31. 增加地表装饰。
32. 增加沙漠仙人掌。
33. 增加矿物随机埋藏。
34. 增加随机动物生成。
35. 增加牛、羊、狐狸、兔子、鸟模型。
36. 增加动物基础游走逻辑。
37. 增加动物远离玩家后自动清理。
38. 增加雨、雪、暴雨、晴天、自动天气模式。
39. 增加天气粒子系统。
40. 增加暴雨时能见度降低。
41. 增加昼夜循环。
42. 增加昼夜速度按钮。
43. 增加多个昼夜速度档位。
44. 增加太阳/月光强度变化。
45. 增加日出/日落偏暖色天空。
46. 增加云层和云层移动。
47. 增加本地自动保存。
48. 增加手机端长按连续挖掘/放置。

## 操作入口

- 🎒：物品栏
- ⚒️：合成 / 背包数量
- 💾：保存世界
- 🎲：随机刷新新世界
- ⏱️：调整昼夜速度
- 🌦️：切换天气
- 👁️：调整视野区块距离
- 🏠：回到出生点

## 仍可继续做的方向

- 将轻量生存闭环升级为真正的 3D 掉落物实体和自动拾取。
- 加入工具挖掘速度、采集等级、耐久和真实工作台 UI。
- 加入生命值、饥饿值和伤害。
- 加入更复杂的洞穴和地下结构。
- 加入音效文件和背景音乐。
